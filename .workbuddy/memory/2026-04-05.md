# 2026-04-05 工作记录

## 外贸口语培训小程序扩课
- **新增课程**: Day 37 - 新能源电池包装出口认证说明
- **课程内容**: UN383认证、ANSI/ESD S20.20防静电认证、IATA空运和IMDG海运法规、清关文件准备、当地进口法规建议
- **更新文件**:
  - `c:\Users\Lenovo\WorkBuddy\20260403194237\training_app.html`
  - `D:\优盖特客户数据\外贸口语培训小程序\30天课程库\oral_training_courses_complete.js`（版本更新至3.2）
- **课程总数**: 37天
- **执行时间**: 09:00 自动化任务
- **人工续更**: 追加 Day 38《展会后重点客户跟进与视频会议邀约》，并把网页版课程列表改为按实际课时动态显示、全部课程可点击学习；最新版 HTML 已同步到 D:\优盖特客户数据\外贸口语培训小程序\飞书微应用\training_app.html 并部署到 https://uget-oral-training.netlify.app ，可直接发给同事使用。
- **问题修复**: 修复"示范音频"按钮长期停留在"准备中..."的问题；为 TTS 播放增加启动超时复位、请求状态隔离和 `speechSynthesis.resume()` 恢复处理，最新版 HTML 已重新部署到 https://uget-oral-training.netlify.app ，可直接刷新验证。
- **兼容性优化**: 根据预览截图继续优化示范音频区：新增"当前环境可能不支持系统语音"的页内提示和"复制英文原文"备用按钮；即使内置预览无语音，用户也能直接看到原因并继续练习，公开版已再次部署到 https://uget-oral-training.netlify.app 。
- **音频方案重构**: 将示范音频从浏览器即时 TTS 改为预生成 MP3 静态资源播放；在 `c:\Users\Lenovo\WorkBuddy\20260403194237\training_app.html` 中接入按 Day + 口音自动匹配的音频路径逻辑，新增 `c:\Users\Lenovo\WorkBuddy\20260403194237\generate_demo_audio.py` 批量生成脚本，并生成 `c:\Users\Lenovo\WorkBuddy\20260403194237\assets\audio\day-01` 到 `day-38` 的美式/英式示范音频，后续扩课继续沿用该方案。
- **分享方式确认**: 已确认公开地址 `https://uget-oral-training.netlify.app` 当前可正常访问（HTTP 200）；后续给同事试用时优先发送公网链接，不再发送本地 `127.0.0.1` 预览地址。
- **协作规则固化**: 用户明确要求把"任何方案都先用第一性原理拆解、先做最小化可行性验证、关键结论再次验证、验证通过后再布局和一步步操作"作为长期规则，后续涉及部署、集成、平台接入及其他方案设计时一律按此执行。
- **规则补充强化**: 用户进一步明确：第一性原理不是停留在"先分析"，而是必须把问题拆到最底层，先追问"这件事最根本的事实是什么"，删除经验、跟风和别人做法带来的干扰，再从底层事实重新推导自己的方法；这条要求作为长期协作规则持续生效。
- **分享方案推导**: 针对"把英语口语练习分享给同事"这个问题，后续统一按第一性原理处理：先围绕"同事能否稳定访问、能否正常播示范音频、能否稳定做语音跟读、分发成本是否足够低"四个底层事实判断，再在公网链接、飞书入口跳浏览器、飞书内嵌网页应用、自有域名托管等方案之间做选择，不再先拍脑袋布局。
- **Netlify 可达性再次验证**: 通过外部抓取验证 `https://uget-oral-training.netlify.app` 页面本身在线且可返回正常 HTML，但在当前本地环境用 `curl` 直接访问时出现 `Recv failure: Connection was reset`；说明问题更可能出在当前网络到 Netlify 的链路稳定性，而不是网页文件本身。后续给国内同事分享时，不能只验证"站点在线"，还要验证"当前网络是否可达"。
- **国内托管方案再次验证**: 读取 `training_app.html` 后确认当前小程序仅依赖静态 HTML/CSS/JS、本地 `assets/audio/*.mp3`、浏览器 `SpeechRecognition`/`localStorage`，没有后端接口依赖；基于官方文档再次验证后，当前更适合的国内分享落点是：① 腾讯云 CloudBase 静态网站托管（支持静态资源、默认域名、HTTPS/CDN，最快验证）；② 腾讯云 COS 静态网站托管 + 自定义域名（适合长期正式化，但 2024 年后新桶默认/静态网站域名不适合直接预览，需绑定自定义域名）；③ 公司现有服务器/域名静态部署（控制力最高，但运维成本更高）。
- **无域名场景结论**: 若当前是个人使用且没有自定义域名，从第一性原理推导后应优先选腾讯云 CloudBase 静态网站托管，用平台提供的默认 HTTPS 域名直接分享；不应优先选 COS 静态网站托管，因为其新桶默认/静态网站域名不适合作为正式网页预览入口。
- **最小部署前检查**: 已完成 CloudBase 上线前的结构核对：当前工作区根目录只有 `training_app.html` 而没有 `index.html`，因此上线时必须二选一——要么把首页入口配置到 `training_app.html`，要么将其改名/复制为 `index.html`；页面音频路径使用相对地址 `assets/audio/day-xx-*.mp3`，与现有 76 个 MP3 文件命名完全匹配，只要保持 `assets/audio` 与首页在同一站点根目录即可直接工作；当前唯一外部依赖是 Font Awesome CDN，图标加载失败只影响图标显示，不影响课程、音频和本地进度等核心功能。
- **当前推荐决策**: 若由助手按第一性原理亲自做选择，在"个人使用、无自定义域名、目标是尽快稳定分享给国内同事"的前提下，应选 **CloudBase 静态网站托管 + 平台默认 HTTPS 域名 + 首页统一用 `index.html`**。原因是它同时满足"无需域名、可直接分享、适配纯静态 HTML/JS/MP3、国内访问更稳、平台默认首页行为更确定"五个底层条件；相比之下，继续用 Netlify 受国内链路影响更大，COS 更偏向有域名后的长期正式化方案。
- **首页文件已落地**: 已按最小改动原则在工作区新增 `c:\Users\Lenovo\WorkBuddy\20260403194237\index.html`，其内容直接复制自当前稳定版 `training_app.html`，保留原文件不动；这样后续上传到 CloudBase 时，可直接以工作区根目录作为静态托管根目录，默认首页行为更确定，也不会影响现有本地版本继续维护。
- **最小部署步骤已整理**: 已产出《CloudBase 最小部署步骤》文档，明确当前项目已满足静态托管结构要求；后续真正上线前，用户侧唯一必须完成的前提是"腾讯云账号实名认证 + 创建云开发环境"，然后即可在 CloudBase 控制台直接上传当前工作区根目录，使用默认 HTTPS 域名访问验证。
- **控制台点击清单已整理**: 已基于 CloudBase 官方文档把控制台操作进一步压缩为最短路径：创建环境 → 进入环境 → 左侧"静态网站托管" → "新建部署" → "上传文件夹" → 选择 `c:\Users\Lenovo\WorkBuddy\20260403194237` → "部署" → 打开"默认域名"。后续可直接按此顺序实操，不必再重新研究页面路径。
- **小白逐步指南已产出**: 已为当前项目整理《CloudBase 小白逐步操作指南》，明确采用唯一推荐路径"CloudBase 控制台上传整个工作区文件夹"，并把创建环境、进入静态网站托管、上传文件夹、打开默认域名、上线后最小验证这几步拆成可逐句跟做的操作说明；后续用户只需按当前界面进度回报"到哪一步了"，即可继续一段一段推进。
- **CloudBase 路径已被价格事实否定**: 实操中进一步验证到，当前 CloudBase 免费体验环境无法使用"上传文件夹"，而升级入口直接落到高价企业套餐（当前界面显示约 9999 元/月），这与"个人、小白、低成本、尽快上线"的底层约束冲突，因此该路径不再是当前阶段最优解。
- **新的唯一推荐选择**: 在"不要高价付费、不要自定义域名、纯静态 HTML/JS/MP3、尽快给同事可访问地址"这些底层事实下，下一步应改选 **GitHub Pages**：它能免费托管公开静态站、默认提供 `github.io` 域名和 HTTPS；Netlify 已验证国内链路不稳，不适合作为唯一首选；腾讯云 COS 官方文档已再次验证 2024 年后新桶若无自定义域名则不适合作为正式静态网站访问入口，所以现阶段也不如 GitHub Pages 省事。
- **部署方案阶段性结论汇总**: 以"低成本、无自定义域名、纯静态站、尽快给同事可访问链接"为底层约束重新汇总后，最近尝试的方法里：CloudBase 控制台本地上传文件夹这条路被套餐限制和价格事实否定；CloudBase 现有应用链接可打开安全页但最终 404，说明当前已部署内容不完整；Netlify 技术上能发布，但当前网络到 Netlify 的链路稳定性不足，不适合当唯一稳定入口；COS 在无自定义域名前提下不适合作为正式静态站入口。现阶段真正保留为可走通主路线的，只剩 **GitHub Pages**，前提是先补一个 GitHub 账号。
- **后续协作方式确认**: 用户在明确结论后要求继续按"一步一步操作"的方式推进 GitHub Pages 路线；后续应继续采用一次只给当前一步、用户完成再接下一步的带操作协作方式。
- **GitHub Pages 音频失败根因已定位**: 页面本身已成功部署，但示范音频加载失败的根因不是 GitHub Pages 本身，而是上传时把 MP3 文件平铺到了仓库根目录；当前 `index.html` 实际引用路径是 `assets/audio/day-xx-american.mp3` 与 `assets/audio/day-xx-british.mp3`，因此线上页面请求这些地址时会 404。后续修复必须让仓库里存在正确的 `assets/audio/` 目录结构，不能只把音频文件放在根目录。
- **GitHub Pages 已做兼容修复**: 为降低小白用户二次调整目录结构的难度，已直接修改工作区 `c:\Users\Lenovo\WorkBuddy\20260403194237\index.html` 的音频解析逻辑：播放前会优先探测 `assets/audio/day-xx-*.mp3`，若不存在则自动回退到仓库根目录 `day-xx-*.mp3`。这样即便当前 GitHub 仓库里的 MP3 仍平铺在根目录，只要重新上传修复后的 `index.html`，线上示范音频也可恢复。
- **GitHub Pages 再次最小化验证结论**: 已直接在线验证当前公开页与仓库状态：`https://mary202514113-max.github.io/uget-oral-training/day-01-american.mp3` 返回 200、`/assets/audio/day-01-american.mp3` 返回 404，说明根目录音频文件本身在线；但公开页 HTML 与 `main` 分支 `index.html` 都还不包含 `resolveLessonAudioSource` / `getLessonAudioSources` 这次兼容修复代码，且 GitHub API 显示最近一次提交仍停留在 `2026-04-05T09:12:58Z | Add files via upload`，没有出现用户刚才那次新提交。根因已缩小为：问题不在音频文件，而在"修复后的 `index.html` 尚未真正进入仓库主分支"。
- **GitHub 网页内联修复推进到关键点**: 已基于本地已验证代码，指导用户在 GitHub 网页编辑器里直接把 `getLessonAudioSource` 区域改成 3 个函数：`getLessonAudioSources`、`getLessonAudioSource`、`resolveLessonAudioSource`；当前用户已完成代码块粘贴，下一步只需先做文本级验证（确认页面内确实出现 `resolveLessonAudioSource`），再提交保存，避免再次盲提交流水线。
- **GitHub Pages 修复已真正进仓库**: 用户已在 GitHub 网页端成功提交 `Update index.html`；在线再次最小化验证显示：仓库 `main` 分支 raw 文件与 GitHub Pages 公网页都已包含 `resolveLessonAudioSource` 与 `getLessonAudioSources`，最新提交记录为 `2026-04-05T10:32:02Z | Update index.html`。后续只需做最终用户侧播放验证，不必再重复走"是否提交成功"的排查链路。
- **GitHub Pages 音频失败剩余根因已锁定**: 用户刷新后仍失败，再次直接抓取线上 HTML 逐行验证发现：虽然 3 个新函数已上线，但 `playDemoAudio()` 里实际仍写的是 `const audioSource = getLessonAudioSource(lesson, currentAccent);`，没有改成 `await resolveLessonAudioSource(...)`，所以播放时仍只取旧路径、不会触发根目录 MP3 回退。这次剩下的修复只需改这一行，不必再重复检查提交链路或音频文件本身。
- **一次到位修复清单已收敛为 2 行**: 继续对比本地稳定版与 GitHub 当前 raw 文件后确认，远端还差且只差两处：① `function playDemoAudio(rate = 1.0) {` 必须改成 `async function playDemoAudio(rate = 1.0) {`；② `const audioSource = getLessonAudioSource(lesson, currentAccent);` 必须改成 `const audioSource = await resolveLessonAudioSource(lesson, currentAccent);`。除这两行外，本次音频回退修复链路其余部分已在线上就位。
- **当前用户执行指令已压缩到最小动作**: 面向用户的下一步不再做全链路排查，只需在 GitHub 网页编辑器中精确修改 `playDemoAudio` 里的上述两行，再提交并强刷验证；继续保持"一次只给一步"的带操作方式，避免重复折返。
- **两行修复已成功进入仓库主分支**: 用户于 GitHub 网页端再次提交 `Update index.html` 后，在线验证显示仓库 raw 文件已经同时包含 `async function playDemoAudio(rate = 1.0)` 与 `const audioSource = await resolveLessonAudioSource(lesson, currentAccent)`，最新提交时间为 `2026-04-05T10:55:49Z`。但 GitHub Pages 公网页当时尚未同步到这次提交，因此下一步应只做等待 Pages 刷新后再强制刷新验证，不必再回头改代码。
- **GitHub Pages 音频问题已最终修复并完成用户侧验证**: 用户等待 Pages 同步后强制刷新 `https://mary202514113-max.github.io/uget-oral-training/`，页面已出现"停止播放 / 正在播放..."，说明示范音频恢复正常。今日完整复盘结论：主要卡点依次是 `assets/audio/` 与仓库根目录音频结构不一致、兼容修复代码一开始没有真正提交到主分支、提交后 `playDemoAudio()` 内仍残留两行旧调用、以及 GitHub Pages 同步有延迟。后续若再走 GitHub Pages，优先按"先核对仓库目录结构 → 再核对 raw 文件代码 → 再等 Pages 同步后强刷验证"的顺序排查，不再反复盲改。
- **后续维护清单已整理**: 已基于今天踩坑结果整理出可复用的 GitHub Pages 维护顺序：新增课程时优先修改本地 `training_app.html` 与工作区 `index.html`、新增音频后尽量放入 `assets/audio/` 标准目录；每次发布后只检查 5 项——仓库文件结构、raw `index.html` 是否含最新代码、公开页是否同步、课程能否打开、示范音频是否可播放。
- **GitHub Pages 30 秒发版检查清单已沉淀**: 已进一步压缩出可口头直接执行的最短检查顺序：① 看仓库是否已有最新 `index.html` 与对应音频；② 看 raw `index.html` 是否含本次最新改动；③ 等 Pages 同步后 `Ctrl + F5` 强刷公开页；④ 点开课程；⑤ 点"播放示范"确认出现"停止播放 / 正在播放..."。后续用户若说"发版前帮我过一遍"，可直接按这 5 步带走。
- **新增课程 5 步模板已整理**: 已为该口语练习小程序沉淀出"新增 Day39/Day40 时的固定动作模板"：先补课程文案，再同步 `training_app.html` 与 `index.html`，再补美式/英式 MP3，随后发版到 GitHub，最后按固定 5 项做课程与音频验证。
- **今日卡点复盘已转长期记忆**: 已把今天 GitHub Pages 修复的长期结论固化到 `MEMORY.md`，重点包括四层发布链路分离验证、异步音频回退机制、Pages 同步延迟、仓库网页编辑器仅适合小改，以及固定排查顺序。
- **定时复盘要求已固化**: 用户要求后续每天晚上 20:15 持续复盘口语小程序当天卡点，并把稳定有效的结论继续沉淀到长期记忆。
后续用户再说"继续加一天"，可直接按这套模板推进。

## 20:15 卡点复盘（自动化）
- 今日口语小程序主要卡点集中在部署链路（8 个卡点，全部已解决）
- 新沉淀 7 条长期规则到 MEMORY.md
- 待观察：GitHub Pages 国内可达性（需同事实测）、课程内容质量反馈
- 明日建议：同事实测 GitHub Pages、Day 39 按 5 步模板推进

---

## 20:15 卡点复盘（2026-04-05 执行）

### 当日状态快照
| 项目 | 状态 |
|------|------|
| 课程总数 | Day 1-38（共38天） |
| 音频资源 | 76个MP3（day-01~38 美式/英式） |
| 主文件 | training_app.html（2317行） |
| 工作区 | c:\Users\Lenovo\WorkBuddy\20260403194237\ |
| 线上部署 | GitHub Pages 已通，音频已修复 |

### 今日新增卡点
**无新增卡点** —— 今日主要工作是修复GitHub Pages音频问题，所有卡点已在白天解决。

### 根因与验证结论
1. **音频路径结构不一致** → 本地用`assets/audio/`，仓库初期平铺根目录
   - 修复：增加异步回退机制`resolveLessonAudioSource`
   - 验证：线上页面已可播放示范音频

2. **修复代码未真正提交** → 用户本地有修改但未进仓库主分支
   - 修复：指导用户在GitHub网页端精确修改2行
   - 验证：raw文件与公网页均已包含新代码

3. **GitHub Pages同步延迟** → 提交后需等待1-2分钟
   - 结论：已沉淀"等待→强刷→验证"规则

### 今日固定动作
- ✅ 完成20:15自动化复盘
- ✅ 创建自动化任务记忆文件
- ✅ 确认MEMORY.md已包含所有长期规则

### 明日建议
- 同事实测GitHub Pages国内可达性
- 如需新增Day 39，按5步模板推进
- 用户新增长期要求：后续必须每天更新新的口语课程；默认仍按上午 09:00 续更节奏执行。

















































































































































